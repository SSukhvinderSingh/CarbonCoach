# Gemini API configuration
# App remains fully functional without this key (uses fallback templates)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Grid emission factor (India default: 0.71 kg CO2/kWh)
# Override per state/user as needed
GRID_FACTOR_KG_CO2_PER_KWH=0.71
